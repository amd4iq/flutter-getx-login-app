// Storage Keys

const String isDarkMode = 'is-dark-mode';

// Check if the user is authenticated
const String isAuthenticated = 'is-authenticated';

// Check if the app is launched for the first time
const String isFirstLaunch = 'is-first-launch';

// Check if the user is signed in
const String isSignedIn = 'is-signed-in';

const String isSignedUp = 'is-signed-up';

const String isSignedInWithGoogle = 'is-signed-in-with-google';

const String isSignedInWithFacebook = 'is-signed-in-with-facebook';

const String signinWithFirebaseEmailAndPasswordKey =
    'signin-with-firebase-email-and-password';
